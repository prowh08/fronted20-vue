let sum = function(...a) {
    let result=0;
    for(let i=0;i<1;++i){
        result+=a[0];
        console.log(result)
    }
    a.reduce
   
    return result;
}

function testsum(f) {
    let arr=[5];
   for(let i=0;i<5;++i)
   arr[i]=Math.floor(Math.random()*11);
   console.log(arr);

   
}

for(let i=0;i<5;++i)
    testsum(sum)